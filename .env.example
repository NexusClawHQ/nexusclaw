DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=nexusclaw
DATABASE_PASSWORD=replace-with-a-long-random-password
DATABASE_NAME=nexusclaw_community
JWT_SECRET=replace-with-at-least-32-random-characters
COMMUNITY_SOURCE_URL=replace-with-public-source-url
PORT=3000
NODE_ENV=production

# --- BYO real-LLM demo path (optional; leave ALL THREE unset to keep the
# deterministic zero-credential scenario). Any OpenAI-compatible
# chat-completions endpoint works: DeepSeek / Qwen / Doubao / Zhipu / vLLM /
# Ollama. Partial configuration refuses to boot (fail-fast, no silent
# downgrade). The key is never logged, returned or stamped.
#COMMUNITY_LLM_BASE_URL=https://api.deepseek.com/v1
#COMMUNITY_LLM_API_KEY=replace-with-your-key
#COMMUNITY_LLM_MODEL=deepseek-chat
